Dear Team,
   Kindly do the following steps to run in your local machine,
   After copying the source code to visual studio, you need to run the json server
   To run the json server, copy the following command and paste it on command prompt of visual studio
   command ->  npm run json-server
   After running the server, open an aditional command prompt / Terminal to run the project.
   If the json server is not running,then the prpject data will not be fetched.